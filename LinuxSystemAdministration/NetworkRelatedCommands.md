### Show Open Ports on System

```bash
netstat -tulpn
```

### nslookup

nslookup is a command-line tool to discover the IP address or DNS record of a specific domain name.

```bash
nslookup [options] [domain-name/ip]
```

